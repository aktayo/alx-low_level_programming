#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * Return: Alway 0
 */

int main(void)
{
	int i;
        Int a;

	for( i = 'a'; i <= z; i++)
	{
		putchar("%c",i)
	}
        for( a = 'A'; i <= Z; A++)
	{
		putchar("%c",a)
	}
	return(0);
}
