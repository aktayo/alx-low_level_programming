#include <stdio.h>
/**
 * main - Entry point
 * Return: Alway 0
 */

int main(void)
int i;
     int a;
	
    a =0;
	for(i = 0;i <= 9; i++)
	{ switch(a = 0) a++;
		putchar (i % 10 + '0');

       switch(a = 1) a++; 
        {putchar (a % 10 + '0');
        
        }
        putchar (',');
		putchar (' ');
	
	}
	putchar ('\n');
	return (0);
}
