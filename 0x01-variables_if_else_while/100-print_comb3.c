#include <stdio.h>
/**
 * main - Entry point
 * Return: Alway 0
 */

int main(void)
	int i;

	for(i = 0;i <= 9; i++)
	{
		int a;
		switch (a = 0) a++;
		putchar (i % 10 + '0');
		switch (a = 1) a++; 
        {
		putchar (a % 10 + '0')
        }
        	putchar (',');
		putchar (' ');
	
	}
		putchar ('\n');
		return (0);
}
