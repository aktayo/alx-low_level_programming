#include "main.h"
/**
 * print_alphabet_x10 - print lowercase 10times
 *
 * Return: 0
 */

void print_alphabet_x10(void)
{
	int i;
	int a;

	for (a = 0; a <= 9; a++)
	{
		i = a;
		while (i <= z)
		{
			putchar (i);
		i++;
		}
		putchar ('\n');
		return (0);
	}
}
